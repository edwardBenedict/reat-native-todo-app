import {StyleSheet} from 'react-native';

export const mainStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'black',
    padding: 8,
  },
  emptyList: {
    color: 'white',
    fontSize: 20,
    padding: 20,
    textAlign: 'center',
  },
});
